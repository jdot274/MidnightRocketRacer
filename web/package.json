{
  "name": "midnight-rocket-racer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Midnight Rocket Racer - moody black-sky neon arcade racer (web vertical slice)",
  "scripts": {
    "dev": "vite --port 8140 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 8141 --strictPort"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
